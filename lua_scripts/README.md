# Lua scripts

The old Worgen helper scripts are retained under `legacy/` only for reference. The integrated C++ module now handles Two Forms gender synchronization and combat form forcing, so Eluna is not required for that behavior.
